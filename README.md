Captures of unsolicited traffic on single instance, binned per day. TCP is filtered out, ICMP is binned separately.

Useful for learning scanning patterns, studing DDoS backscatter and attack requests.
